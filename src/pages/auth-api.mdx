export const description =
  'On this page, we will dive into the different authentication endpoints you can use to manage user authentication and sessions.'

# Authentication API

SonicJs provides a comprehensive set of authentication endpoints to handle user authentication, email confirmation, password resets, and session management. {{ className: 'lead' }}

This includes the ability to:

1. Register new users
2. Login with email/password or OTP
3. Manage user sessions (get current user, logout)
4. Handle password resets
5. Send and receive email confirmations
6. Check email existence and confirmation status

## Login {{ tag: 'POST', label: '/api/v1/auth/login' }}

<Row>
  <Col>
    This endpoint allows you to authenticate a user and receive a session token.

    ### Required attributes

    <Properties>
      <Property name="email" type="string">
        The user's email address
      </Property>
      <Property name="password" type="string">
        The user's password
      </Property>
      <Property name="otp" type="string">
        One-time password (optional - used for one-time password login)
      </Property>
    </Properties>

    ### Return Data

    <Properties>
      <Property name="bearer" type="string">
        The session token to be used in subsequent requests
      </Property>
      <Property name="expires" type="number">
        The expiration timestamp for the session
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/api/v1/auth/login">
    ```json {{ title: 'body' }}
    {
      "email": "user@example.com",
      "password": "userpassword"
    }
    ```
    </CodeGroup>

    ```json {{ title: 'Response (200)' }}
    {
      "bearer": "session-token",
      "expires": 1748293651641
    }
    ```

    ```json {{ title: 'Response (401)' }}
    {
      "error": "Invalid email/password combination"
    }
    ```
  </Col>
</Row>

---

## Register User {{ tag: 'POST', label: '/api/v1/users' }}

<Row>
  <Col>
    This endpoint allows you to register a new user in the system.

    ### Required attributes

    <Properties>
      <Property name="email" type="string">
        The user's email address
      </Property>
      <Property name="password" type="string">
        The user's password
      </Property>
      <Property name="firstName" type="string">
        The user's first name
      </Property>
      <Property name="lastName" type="string">
        The user's last name
      </Property>
    </Properties>

    ### Optional attributes

    <Properties>
      <Property name="role" type="string">
        The user's role (defaults to "user")
      </Property>
      <Property name="profile" type="object">
        Additional user profile information
      </Property>
    </Properties>

    ### Return Data

    <Properties>
      <Property name="data" type="object">
        The created user data
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/api/v1/users">
    ```json {{ title: 'body' }}
    {
      "email": "user@example.com",
      "password": "userpassword",
      "firstName": "John",
      "lastName": "Doe",
      "role": "user",
      "profile": {
        "company": "Example Corp",
        "position": "Developer"
      }
    }
    ```
    </CodeGroup>

    ```json {{ title: 'Response (201)' }}
    {
      "data": {
        "id": "2026ad2d-2dc2-4187-9809-7ade12d621b1",
        "email": "user@example.com",
        "firstName": "John",
        "lastName": "Doe",
        "role": "user",
        "profile": {
          "company": "Example Corp",
          "position": "Developer"
        },
        "createdOn": 1748293651641,
        "updatedOn": 1748293651641
      }
    }
    ```

    ```json {{ title: 'Response (400)' }}
    {
      "error": "Email already exists"
    }
    ```

    ```json {{ title: 'Response (400)' }}
    {
      "error": "Invalid email format"
    }
    ```
  </Col>
</Row>

---

## Current User {{ tag: 'GET', label: '/api/v1/auth/user' }}

<Row>
  <Col>
    This endpoint returns information about the current user session.

    ### Required Headers

    <Properties>
      <Property name="Authorization" type="string">
        Bearer token for authentication
      </Property>
    </Properties>

    ### Return Data

    <Properties>
      <Property name="data" type="object">
        Contains user and session information
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/api/v1/auth/user">
    ```bash {{ title: 'cURL' }}
    http://localhost:4321/api/v1/auth/user -H "Authorization: Bearer session-token"
    ```
    </CodeGroup>

    ```json {{ title: 'Response (200)' }}
    {
      "data": {
        "user": {
          "id": "2026ad2d-2dc2-4187-9809-7ade12d621b1",
          "email": "user@example.com",
          "firstName": "John",
          "lastName": "Doe",
          "role": "user",
          "profile": {}
        },
        "session": {
          "userId": "2026ad2d-2dc2-4187-9809-7ade12d621b1",
          "activeExpires": 1748293651641,
          "idleExpires": 1748293651641,
          "token": "session-token"
        }
      }
    }
    ```

    ```json {{ title: 'Response (401)' }}
    {
      "error": "No token provided or invalid token"
    }
    ```
  </Col>
</Row>

---

## Logout {{ tag: 'POST', label: '/api/v1/auth/logout' }}

<Row>
  <Col>
    This endpoint invalidates the current session.

    ### Required Headers

    <Properties>
      <Property name="Authorization" type="string">
        Bearer token for authentication
      </Property>
    </Properties>

    ### Return Data

    <Properties>
      <Property name="message" type="string">
        Success message
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/api/v1/auth/logout">
    ```bash {{ title: 'cURL' }}
    http://localhost:4321/api/v1/auth/logout -H "Authorization: Bearer session-token"
    ```
    </CodeGroup>

    ```json {{ title: 'Response (200)' }}
    {
      "message": "Successfully logged out"
    }
    ```
  </Col>
</Row>

---

## Password Reset {{ tag: 'GET', label: '/api/v1/auth/password/reset/send/{email}' }}

<Row>
  <Col>
    This endpoint sends a password reset email to the specified email address.

    ### Required attributes

    <Properties>
      <Property name="email" type="string">
        URL-encoded email address to send reset link to
      </Property>
    </Properties>

    ### Return Data

    <Properties>
      <Property name="message" type="string">
        Success message
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/api/v1/auth/password/reset/send/user%40example.com">
    ```bash {{ title: 'cURL' }}
    http://localhost:4321/api/v1/auth/password/reset/send/user%40example.com
    ```
    </CodeGroup>

    ```json {{ title: 'Response (200)' }}
    {
      "message": "If the email address is valid, a password reset email has been sent"
    }
    ```
  </Col>
</Row>

---

## Password Reset Receive {{ tag: 'POST', label: '/api/v1/auth/password/reset/receive' }}

<Row>
  <Col>
    This endpoint resets the user's password using a reset code.

    ### Required attributes

    <Properties>
      <Property name="email" type="string">
        User's email address
      </Property>
      <Property name="password" type="string">
        New password
      </Property>
      <Property name="code" type="string">
        Password reset code from email
      </Property>
    </Properties>

    ### Return Data

    <Properties>
      <Property name="message" type="string">
        Success message
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/api/v1/auth/password/reset/receive">
    ```json {{ title: 'body' }}
    {
      "email": "user@example.com",
      "password": "newpassword",
      "code": "reset-code"
    }
    ```
    </CodeGroup>

    ```json {{ title: 'Response (200)' }}
    {
      "message": "Password updated successfully"
    }
    ```

    ```json {{ title: 'Response (400)' }}
    {
      "error": "Invalid reset code, expired code, or missing fields"
    }
    ```
  </Col>
</Row>

---

## One-Time Password (OTP) {{ tag: 'GET', label: '/api/v1/auth/password/otp/{email}' }}

<Row>
  <Col>
    This endpoint sends a one-time password to the specified email address.

    ### Required attributes

    <Properties>
      <Property name="email" type="string">
        URL-encoded email address to send OTP to
      </Property>
    </Properties>

    ### Return Data

    <Properties>
      <Property name="message" type="string">
        Success message
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/api/v1/auth/password/otp/user%40example.com">
    ```bash {{ title: 'cURL' }}
    http://localhost:4321/api/v1/auth/password/otp/user%40example.com
    ```
    </CodeGroup>

    ```json {{ title: 'Response (200)' }}
    {
      "message": "OTP sent successfully"
    }
    ```

    ```json {{ title: 'Response (404)' }}
    {
      "error": "User not found"
    }
    ```
  </Col>
</Row>

---

## Email Confirmation {{ tag: 'GET', label: '/api/v1/auth/email-confirmation/send/{email}' }}

<Row>
  <Col>
    This endpoint sends an email confirmation link to the specified email address.

    ### Required attributes

    <Properties>
      <Property name="email" type="string">
        URL-encoded email address to send confirmation to
      </Property>
    </Properties>

    ### Return Data

    <Properties>
      <Property name="message" type="string">
        Success message
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/api/v1/auth/email-confirmation/send/user%40example.com">
    ```bash {{ title: 'cURL' }}
    http://localhost:4321/api/v1/auth/email-confirmation/send/user%40example.com
    ```
    </CodeGroup>

    ```json {{ title: 'Response (200)' }}
    {
      "message": "Confirmation email sent"
    }
    ```
  </Col>
</Row>

---

## Email Confirmation Receive {{ tag: 'GET', label: '/api/v1/auth/email-confirmation/receive/{code}' }}

<Row>
  <Col>
    This endpoint confirms the user's email address using the confirmation code.

    ### Required attributes

    <Properties>
      <Property name="code" type="string">
        Email confirmation code
      </Property>
    </Properties>

    ### Return Data
    Redirects to the configured `EMAIL_CONFIRMATION_REDIRECT_URL`

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/api/v1/auth/email-confirmation/receive/confirmation-code">
    ```bash {{ title: 'cURL' }}
    http://localhost:4321/api/v1/auth/email-confirmation/receive/confirmation-code
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Email Existence Check {{ tag: 'GET', label: '/api/v1/auth/email-exists/{email}' }}

<Row>
  <Col>
    This endpoint checks if an email address exists in the system and if it has been confirmed.

    ### Required attributes

    <Properties>
      <Property name="email" type="string">
        URL-encoded email address to check
      </Property>
    </Properties>

    ### Return Data

    <Properties>
      <Property name="exists" type="boolean">
        Whether the email exists in the system
      </Property>
      <Property name="confirmed" type="boolean">
        Whether the email has been confirmed
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/api/v1/auth/email-exists/user%40example.com">
    ```bash {{ title: 'cURL' }}
    http://localhost:4321/api/v1/auth/email-exists/user%40example.com
    ```
    </CodeGroup>

    ```json {{ title: 'Response (200)' }}
    {
      "exists": true,
      "confirmed": true
    }
    ```
  </Col>
</Row>

---

## Security Notes

The authentication system implements several security measures:

1. All endpoints use secure session management with expiration times
2. Passwords are hashed using Argon2
3. Session tokens are generated using secure random strings
4. Email confirmation and password reset links have expiration times
5. OTP codes are single-use and expire after a configured time period
6. The system supports invalidating all user sessions on login if configured

---

## Environment Variables

The following environment variables are used by the authentication system:

<Properties>
  <Property name="REQUIRE_EMAIL_CONFIRMATION" type="boolean">
    Enable/disable email confirmation requirement
  </Property>
  <Property name="EMAIL_CONFIRMATION_REDIRECT_URL" type="string">
    URL to redirect after email confirmation
  </Property>
  <Property name="ONE_TIME_PASSWORD_EXPIRATION_TIME" type="number">
    OTP expiration time in milliseconds
  </Property>
  <Property name="RESET_PASSWORD_EXPIRATION_TIME" type="number">
    Password reset link expiration time
  </Property>
  <Property name="INVALIDATE_USER_SESSIONS" type="boolean">
    Whether to invalidate all user sessions on login
  </Property>
  <Property name="EMAIL_FROM" type="string">
    Email address for system emails
  </Property>
  <Property name="EMAIL_BASE_URL" type="string">
    Base URL for email links
  </Property>
</Properties>

---

## Next Steps

The authentication endpoints provide you with a solid foundation for user management. For more advanced features, you may want to check out the docs for SonicJS [routing](/routing) to create custom authentication flows.

